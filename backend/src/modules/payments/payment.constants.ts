/** Mirrors the frontend's `PAYMENT_METHODS` in `src/types/student.ts` exactly — same reasoning as student.constants.ts. */
export const PAYMENT_METHODS = ["নগদ", "বিকাশ", "নগদ (মোবাইল)", "রকেট", "ব্যাংক", "অন্যান্য"] as const;
